'use client';

export default function BackgroundScene() {
  return (
    <div className="fixed inset-0 w-full h-full bg-[#ffffff] z-0">
      {/* Optional subtle depth (recommended) */}
    </div>
  );
}